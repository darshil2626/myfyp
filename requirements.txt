# Active runtime dependencies for the SINN FYP pipeline.
# (LEGACY/ scripts use additional packages — torch, xarray, earthaccess,
#  openaq, requests, pandas — that are not required to reproduce FYP results.)
numpy==1.26.2
scipy==1.11.4
matplotlib==3.8.2
tensorflow==2.20.0
keras==3.13.2
